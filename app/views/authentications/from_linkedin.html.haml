#maincontent
  %h2
    You're almost done!  Please fill out the following fields below:
  = form_for @user, :url => finalize_linkedin_authentications_path, :html => {:method => :post, :multipart => true} do |f|
    = hidden_field_tag "uid", @uid
    %p
      First name:
      %br/
      = f.text_field :first_name
    %p
      Last name:
      %br/
      = f.text_field :last_name
    %p
      Email:
      %br/
      = f.email_field :email
    %p
      Choose a profile picture (recommended):
      %br/
      = file_field_tag "avatar"
    %p= submit_tag "Update"