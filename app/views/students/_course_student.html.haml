-if !course_student.details.blank?
  .hover.tTip{:title => "#{course_student.details}", :reference_type => "CourseStudent", :reference_id => course_student.id, :id => "CourseStudent#{course_student.id}" }
    %span
      = link_to course_student.term.name, course_student.term
      %span.delete_course_button{:style => "display:none"}
        = link_to "[X]", delete_term_s_path(:id => course_student.term_id)
      %br/
      .hidden_detail{:id => "course_comment_#{course_student.id}", :style => "display:none" }
-else
  .hover{:reference_type => "CourseStudent", :reference_id => course_student.id, :id => "CourseStudent#{course_student.id}" }
    %span
      = link_to course_student.term.name, course_student.term
      %span.delete_course_button{:style => "display:none"}
        = link_to "[X]", delete_term_s_path(:id => course_student.term_id)
      %br/
      .hidden_detail{:id => "course_comment_#{course_student.id}", :style => "display:none" }
      
